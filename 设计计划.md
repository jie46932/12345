# 12345 智能升降桌 3D 配置器 — 设计计划

**版本**：v1.1  
**日期**：2026-04-16  
**基于**：源码反推 + PRD v1.0 + 竞品分析 + 四天开发实战记录

---

## 一、产品定位

| 项目 | 内容 |
|------|------|
| 产品名称 | 智能升降桌 3D 配置器（HE FURNITURE） |
| 客户 | HE FURNITURE |
| 目标用户 | B端企业采购 + C端消费者 |
| 核心价值 | 购前直观体验桌高/材质/配件组合，减少沟通成本，配置即销售 |
| 使用场景 | 官网嵌入 / 展会演示 / 销售人员演示 |
| 部署平台 | Vercel（线上） + Verge3D App Manager（本地调试） |
| 线上访问 | https://12345-two-rose.vercel.app/ |

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 3D 引擎 | Verge3D 4.11（Three.js 底层） | glTF 加载、相机控制、PBR 渲染 |
| 前端框架 | React 19 + Vite 8 | 组件化 UI 层 |
| 样式方案 | CSS-in-JS（styled-components / template literal） + Tailwind CSS | 主要用 CSS 字面量写在组件内 |
| 字体 | Orbitron 800（品牌/数字） + Rajdhani 600（UI 文字） | Google Fonts |
| 构建输出 | `dist/` → Vercel 读取 | `npm run build:vercel` |
| 3D 模型 | 3ds Max → glTF 2.0（`12345.gltf` + `12345.bin`） | 含动画轨道 + PBR 材质 |

---

## 三、当前组件清单

| 组件 | 文件 | 功能 |
|------|------|------|
| `App.jsx` | `src/App.jsx` | 主容器：Verge3D 集成、高度动画、材质切换、状态提升 |
| `Header` | `src/components/Header.jsx` | 品牌名逐字动画（Orbitron）、右上角 hover 展开按钮组（灯/语言/全屏） |
| `LoadingScreen` | `src/components/LoadingScreen.jsx` | 全屏加载屏，neumorphism spinner + 进度数字，最短展示 2.5s |
| `ControlBar` | `src/components/ControlBar.jsx` | 底部主控栏：高度/标注/分享/材质/配件/购物车 |
| `ViewSlider` | `src/components/ViewSlider.jsx` | 固定左下角视角圆盘（前/后/左/右/俯视） |
| `DimensionAnnotation` | `src/components/DimensionAnnotation.jsx` | SVG 尺寸标注，每帧 `v.project(camera)` 实时跟随 3D 点 |
| `GalleryModal` | `src/components/GalleryModal.jsx` | 画廊弹窗（Apple Carousel 风格） |
| `CartModal` | `src/components/CartModal.jsx` | 购物车弹窗：材质/配件/价格明细 |
| `LangContext` | `src/LangContext.js` | 全局中/英语言切换，翻译字典 |

---

## 四、视觉风格系统

### 4.1 风格定位

**当前实现风格**：暗色豪华 × Neumorphism（与 PRD v1.0 温暖北欧风格不同，已在实现中演进为更有科技感的方向）

| 维度 | 当前值 |
|------|--------|
| 背景 | 深色 + 3D 实时渲染画面（无固定背景色） |
| 控制栏底色 | `rgba(204, 208, 212, 0.36)` 毛玻璃 |
| 毛玻璃效果 | `backdrop-filter: blur(18px) saturate(1.4)` |
| 边框 | `1px solid rgba(255,255,255,0.45)` 高光描边 |
| 按钮风格 | Neumorphism 凸起圆形（inset shadow 凹陷/凸起状态变换） |
| 字体主色 | `rgba(0,0,0,0.72)` 深灰 |
| 字体辅助 | `rgba(0,0,0,0.55)` / `rgba(0,0,0,0.38)` |
| 圆角 | 按钮 20px，控制栏 20px |

### 4.2 字体规范

| 用途 | 字体 | 规格 |
|------|------|------|
| 品牌名 HE FURNITURE | Orbitron 800 | 44px，金属渐变 + 3D drop-shadow |
| 副标题 智能升降桌 | Rajdhani 600 | 21.6px |
| 总价数字 | Orbitron 800 | 44px |
| UI 标签 / 按钮文字 | Rajdhani 400–600 | 13–18px |
| 价格单位 | Rajdhani | 18px |

### 4.3 颜色系统（Neumorphism 基础色）

| 角色 | 色值 |
|------|------|
| 控制面板基色 | `#ccd0d4` |
| 凸起高光 | `rgba(255,255,255,0.8)` |
| 凹陷阴影 | `rgba(0,0,0,0.4)` |
| 毛玻璃底色 | `rgba(204,208,212,0.36)` |
| 文字主 | `rgba(0,0,0,0.72)` |
| 文字辅 | `rgba(0,0,0,0.55)` |

### 4.4 动效规范

| 交互 | 时长 | 缓动 |
|------|------|------|
| 按钮按压（凹陷） | 即时（CSS :active） | — |
| 按钮状态过渡（box-shadow/transform） | 300ms | `cubic-bezier(0.23,1,0.32,1)` |
| 控制栏展开（max-width 动画） | 400ms | `cubic-bezier(0.23,1,0.32,1)` |
| 控制栏展开 opacity | 300ms | ease |
| 相机切换视角 tween | 600ms | Verge3D 内置 |
| 桌高平滑移动（预设切换） | ~670ms（SPEED=1.5 t/s） | 线性插值 |
| 桌高连续移动（箭头） | 持续（SPEED=0.3 t/s） | renderCallback 驱动 |
| 品牌名逐字弹出 | 16s 循环 | letterUp keyframes |
| 加载屏 spinner | 持续旋转 | linear |

---

## 五、可配置功能清单

| 功能 | 状态 | 技术实现 |
|------|------|---------|
| 桌面材质切换（3色） | ✅ 已实现 | 遍历场景找 PBR 材质名，赋给 Rectangle005/006 |
| 桌高预设切换（3档） | ✅ 已实现 | `applyT(t)` 直接插值 Dummy003/002 的 y 坐标 |
| 桌高连续箭头调节 | ✅ 已实现 | `arrowMoveRef` + `renderCallback` 驱动，切换式（再点停止） |
| 配件开关（水杯/挂钩/台灯） | ✅ 已实现 | 设置 `对象010/011/组007.visible` |
| 视角切换（前/后/左/右/俯视） | ✅ 已实现 | `controls.tween()` 相机插值 |
| 尺寸标注显示/隐藏 | ✅ 已实现 | `showAnnotations` 控制 DimensionAnnotation 组件 |
| SVG 尺寸标注实时投影 | ✅ 已实现 | 每帧 `v.project(camera)`，NDC → 屏幕坐标 |
| 分享 QR 码 | ✅ 已实现 | `api.qrserver.com` 动态生成当前 URL 二维码 |
| 画廊弹窗 | ✅ 已实现 | GalleryModal 组件 |
| 购物车 / 价格明细 | ✅ 已实现 | CartModal，基础价 ¥899 + 配件加价 |
| 中/英双语切换 | ✅ 已实现 | LangContext + T 字典 |
| 全屏切换 | ✅ 已实现 | `document.documentElement.requestFullscreen()` |
| 加载进度 | ✅ 已实现 | v3d 进度事件 + 假进度兜底（100ms +1%） |
| 地面反射 | ✅ 已实现 | CubeCamera + MeshStandardMaterial 实时反射 |
| AR 预览 | ⚠️ UI 已有按钮，功能待接入 | 需要 WebXR 或第三方 AR SDK |
| 自动演示模式 | ❌ 待实现 | 从 t=0 自动播到 t=1 再回，突出升降核心卖点 |
| 材质纹理色板 | ❌ 待实现 | 当前为纯色圆形，参考 Oakywood 实物纹理图 |
| 移动端适配 | ⚠️ 基础响应式（`--ui-scale`），触控未专项优化 | 需要 44px 触控目标尺寸验证 |

---

## 六、已知 Bug 与坑位（四天开发实战总结）

| Bug | 根因 | 修复状态 |
|-----|------|---------|
| @keyframes 命名冲突（letterUp） | LoadingScreen 与 Header 定义同名 keyframe，卸载后动画中断 | ✅ 已修复 |
| React StrictMode 破坏加载屏 | 双挂载导致 setLoadingVisible(false) 在第二次 mount 立即触发 | ✅ 已修复（移除 StrictMode） |
| overflow:hidden 内 drop-shadow 黑色方块 | 大半径 drop-shadow 在 overflow:hidden 内叠加为实色块 | ✅ 已修复（只保留 0px 模糊轮廓阴影） |
| canvas.width vs canvas.clientWidth | WebGL 物理分辨率 ≠ CSS 显示尺寸，SVG 投影坐标偏移 2 倍 | ✅ 已修复（统一用 clientWidth） |
| 配件 Toggle 状态不同步 | 3D 初始 visible=true，React 初始 state = 空 Set | ✅ 已修复（onSceneReady 统一初始隐藏） |
| Vite 不重新编译源码 | rollupOptions.input 指向预编译 bundle 路径 | ✅ 已修复（改为 index.dev.html） |
| SVG 标注背面显示 | project() 后 ndc.z > 1 在相机背后仍显示 | ✅ 已修复（z > 1 时 opacity=0） |
| Git LFS 与 Vercel 不兼容 | Vercel 拉 LFS 指针文件，浏览器报 `Unexpected identifier 'https'` | ✅ 已修复（migrate export 转回普通对象） |

---

## 七、竞品差距分析

| 维度 | 国际标杆（Oakywood） | 我们当前 | 差距 |
|------|---------------------|----------|------|
| 3D 渲染质量 | 高精度 PBR，专业摄影级 | Verge3D PBR，中高质量 | 可接受 |
| 高度 3D 动画 | ❌ 无 | ✅ 实时插值，领先 | **全球领先** |
| 材质色板 | 实物纹理图（木纹清晰可见） | 纯色圆形 swatch | 需改进 |
| AR 预览 | ✅ 手机扫码 | ❌ UI 有，功能无 | 中期目标 |
| 自动演示模式 | ❌ 无 | ❌ 无 | **首创机会** |
| 价格实时联动 | ✅ 每次选项 <200ms 更新 | ✅ 购物车明细有，主界面常驻显示弱 | 可优化 |
| 移动端触控 | 拇指区设计，44px 目标 | 基础响应式，未专项优化 | 需专项优化 |
| 分步引导 | 无（自由配置） | 无 | — |
| 国内先发 | — | ✅ 国内唯一实时 3D 升降桌配置器 | **护城河** |

---

## 八、优先级改进路线图

### P0 — 立即可做（1 天内，无需 3D 改模型）

1. **材质色板升级为纹理图**
   - 将 `materials` 数组中的 `color` 字段改为木纹贴图 URL
   - ControlBar 中把 `nb-circle` 的 `background` 从纯色改为 `background-image: url(...)`
   - 参考尺寸：46×46px 圆形裁切，木纹纹理
   
2. **主界面常驻价格显示**
   - 在 cb-bar 右侧已有 `cb-price-wrap`（Orbitron 44px 显示总价）
   - 验证其是否在不展开时也可见，若被折叠则调整布局始终显示

3. **移动端触控尺寸验证**
   - 所有按钮检查是否达到 44×44px（当前 cb-bar 按钮已是 67×67px，符合）
   - 检查 `--ui-scale` 缩小后实际 px 值是否仍 ≥ 44px

### P1 — 近期目标（1 周内）

4. **自动演示模式**
   - 新增"演示"按钮（或 Header 右上角按钮组新增）
   - 点击后：`playToFrame(68)` → 延迟 → `playToFrame(120)` → 延迟 → `playToFrame(94)` 循环
   - 配合 ControlBar 高度数字实时更新，体现"升降桌"核心卖点
   
5. **AR 功能接入**
   - 评估方案：Model Viewer（Google，免费） / Reality Web（付费）
   - 最低成本方案：`<model-viewer ar>` 标签，仅需提供 `.glb` 文件
   - 点击 AR 按钮 → 弹出 Model Viewer 或跳转 AR 页面

6. **加载屏背景图**
   - LoadingScreen 当前为纯深色背景，需要设计一张高质量产品背景图
   - 尺寸：1920×1080，`background-size: cover`，与品牌调性一致

### P2 — 中期规划

7. **配置分享链接**
   - 将当前选择（材质/高度/配件）序列化为 URL 参数
   - 分享按钮复制链接，QR 码已动态生成当前 URL（已具备基础）
   
8. **首次使用引导**
   - 首次访问展示 tooltip：点击底部栏可配置材质/高度
   - 用 localStorage 标记是否展示过

9. **键盘快捷键支持**
   - 数字键 1/2/3：切换高度档位
   - M 键：切换材质
   - A 键：切换视角
   - 这是差异化 UX 细节，对 B 端演示场景（销售人员演示）价值高

---

## 九、技术工作流规范（基于实战经验）

### 9.1 构建系统

```bash
# 开发
npm run dev              # Vite dev server，入口 index.dev.html

# 生产构建（Vercel）
npm run build:vercel     # vite build + 拷贝所有运行时文件到 dist/

# 验证构建是否真正编译了源码
# vite build 输出应显示 39+ modules，若只有 5 modules 说明用了缓存
```

**核心规则**：`rollupOptions.input` 必须指向 `index.dev.html`（引用 `/src/main.jsx`），不能指向 `index.html`（已含预编译 bundle）。

### 9.2 Verge3D 集成要点

```js
// 获取 v3d app 实例（双路 fallback）
if (globalApp || window.v3dApp) {
  onSceneReady({ detail: { app: globalApp || window.v3dApp } });
  return;
}
window.addEventListener('v3d-scene-ready', onSceneReady, { once: true });

// 配件初始化：onSceneReady 里统一隐藏，与 React state 对齐
['对象010', '对象011', '组007'].forEach(name => {
  const obj = app.scene?.getObjectByName(name);
  if (obj) obj.visible = false;
});

// 高度动画：绕过 AnimationAction，直接插值
const applyT = (t) => {
  DUMMIES.forEach(({ name, y0, y1 }) => {
    const obj = app.scene?.getObjectByName(name);
    if (obj) obj.position.y = y0 + (y1 - y0) * t;
  });
};
```

### 9.3 SVG 标注实现速查

```js
// 1. 检测加载完成
document.querySelector('[class*="ls-hide"]')
// 2. 获取世界坐标
obj.getWorldPosition(vec3)
// 3. 投影
vec3.project(camera)  // → NDC
// 4. 转屏幕（必须用 clientWidth！）
x = (ndc.x + 1) / 2 * canvas.clientWidth
y = (1 - ndc.y) / 2 * canvas.clientHeight
// 5. 背面检测
if (ndc.z > 1) 隐藏标注
// 6. 视口溢出 clamp
x = Math.max(MARGIN, Math.min(w - MARGIN, x))
// 7. 每帧驱动
requestAnimationFrame(update)
```

### 9.4 Neumorphism 按钮系统

所有按钮均由「凹槽容器 + 凸起圆形」两层构成：

```
.nb-wrap（凹槽底座，transparent background）
  └── .nb-circle（凸起圆形，blur(1px) + inset shadow）
  └── .nb-icon（图标，absolute 叠在圆形上，不受 blur 影响）
```

状态变换只改 `nb-circle` 的 `box-shadow` + `transform: translateY(1px)`，不改 `nb-wrap`。

---

## 十、UI/UX 验收标准（对照 ui-ux-pro-max 规则）

| 规则 | 检查项 | 当前状态 |
|------|--------|---------|
| `touch-target-size` | 所有交互元素 ≥ 44×44px | ✅ cb-bar 按钮 67×67px |
| `touch-spacing` | 按钮间距 ≥ 8px | ✅ gap:8-12px |
| `loading-states` | >300ms 操作有加载反馈 | ✅ LoadingScreen + 进度 |
| `color-contrast` | 文字对比度 ≥ 4.5:1 | ⚠️ 需用工具验证毛玻璃下对比度 |
| `focus-states` | 交互元素有可见焦点环 | ⚠️ 已移除所有 focus outline（影响键盘可访问性） |
| `aria-labels` | 图标按钮有 title 属性 | ✅ 所有 NeuBtn 有 title prop |
| `reduced-motion` | 支持 `prefers-reduced-motion` | ❌ 未实现，需添加 |
| `mobile-first` | 移动端先行 | ⚠️ 基础响应式，触控未专项测试 |
| `empty-states` | 无内容时有提示 | ✅ 配件默认全关，有按钮引导 |
| `error-feedback` | 操作失败有反馈 | ⚠️ 购物车只有 alert，需优化 |
| `animation-duration` | 150–300ms 微交互 | ✅ 300ms cubic-bezier |
| `transform-performance` | 只动 transform/opacity | ✅ 动画只改 box-shadow/transform |

**需要修复的无障碍问题**：
1. 添加 `prefers-reduced-motion` 媒体查询，减少/停止动效
2. 焦点管理：键盘用户点击后 `blur()` 正确，但需确保键盘 Tab 导航可用
3. `onAddToCart` 目前是 `alert()`，需替换为 toast 或 CartModal

---

## 十一、待解决的开放问题

| 问题 | 优先级 | 备注 |
|------|--------|------|
| AR 功能技术选型 | P1 | Model Viewer vs Reality Web vs WebXR |
| 移动端触控测试 | P1 | 需要真机测试 iOS/Android |
| 加载屏背景图设计 | P1 | 需要设计资源 |
| 木纹材质色板小图 | P0 | 需要 3 张截图/渲染图 |
| `prefers-reduced-motion` 支持 | P1 | CSS + JS 双层 |
| 购物车「加入」后跳转逻辑 | P1 | 当前 alert()，需产品确认跳转目标 |
| 3D 场景灯光调节 | P2 | LangToggle 按钮（灯光开关）目前控制 `lightOn` state，但 3D 灯光实际未联动 |
| 价格常驻显示位置 | P0 | 验证 cb-bar 右侧价格在折叠态是否可见 |

---

## 十二、文件结构

```
F:\verge3d app manager\12345\
├── index.dev.html          # 开发入口（引用 /src/main.jsx）
├── index.html              # 生产入口（构建后自动生成）
├── 12345.gltf / .bin       # 3D 模型（Verge3D 导出）
├── 12345.js                # Verge3D 运行时初始化脚本
├── v3d.js                  # Verge3D 核心库（3.6MB）
├── visual_logic.js / .xml  # Puzzles 逻辑
├── media/                  # 图片资源
├── src/
│   ├── main.jsx            # React 入口（无 StrictMode）
│   ├── App.jsx             # 主组件
│   ├── App.css             # 基础样式
│   ├── critical-fixes.css  # 关键 bug 修复样式
│   ├── LangContext.js      # 国际化 Context + 翻译字典
│   └── components/
│       ├── Header.jsx
│       ├── ControlBar.jsx
│       ├── LoadingScreen.jsx
│       ├── DimensionAnnotation.jsx
│       ├── ViewSlider.jsx
│       ├── GalleryModal.jsx
│       └── CartModal.jsx
├── vite.config.js          # 含 serveRootFiles + fixAssetsPath 插件
├── package.json
├── vercel.json             # buildCommand + rewrites
└── 设计计划.md             # 本文档
```

---

## 十三、设计决策记录（ADR）

| 决策 | 结论 | 理由 |
|------|------|------|
| 视觉风格 | Neumorphism 毛玻璃 而非 PRD 的温暖北欧风 | 科技感更强，与 3D 渲染风格协调，体现产品高端定位 |
| 加载屏控制方式 | CSS class 控显隐 而非条件渲染 | 避免 React StrictMode 双挂载立即消失 |
| 高度动画 | 直接插值 Dummy003/002 的 y 坐标 而非 AnimationAction | AnimationAction 反向播放有兼容性问题，直接插值更可控 |
| 箭头按钮交互 | 切换模式（再点停止）而非长按 | 移动端长按不可靠，切换模式体验更好 |
| 配件初始化 | onSceneReady 统一初始隐藏 | 建模师设置的初始 visible 与 React state 不一致 |
| SVG 标注 | 实时投影（每帧）而非首帧锁定 | 随相机旋转精准跟随，工程感更强 |
| 语言切换 | lang state 提升到 App.jsx | 全局状态必须在最高层持有，否则 Header 切换 ControlBar 不响应 |
| 全屏 API | Fullscreen API 而非 window.resizeTo | 现代浏览器安全策略禁用 resizeTo |
| Git LFS | 不使用 LFS，所有文件普通 git 对象 | Vercel 不支持 LFS，会拉取指针文件导致白屏 |

---

*生成时间：2026-04-16 | 下次更新建议：下一个功能迭代完成后*
